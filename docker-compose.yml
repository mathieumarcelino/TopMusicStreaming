version: '3.8'

networks:
  proxy:
    external: true

services:
  music-app:
    container_name: music-app
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - proxy
